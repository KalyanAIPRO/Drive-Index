{
  "name": "drive-index",
  "version": "1.0.1",
  "description": "Config-driven Google Apps Script that indexes any Google Drive folder into a formatted, clickable spreadsheet inventory.",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "test": "node test/harness.js",
    "deploy": "clasp push --force",
    "bootstrap": "bash bootstrap.sh",
    "open": "clasp open-container",
    "logs": "clasp tail-logs"
  },
  "engines": {
    "node": ">=18"
  }
}
